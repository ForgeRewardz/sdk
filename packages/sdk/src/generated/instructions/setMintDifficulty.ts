/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findGlobalConfigPda } from "../pdas";
import { REWARDZ_MVP_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MINT_DIFFICULTY_DISCRIMINATOR = 3;

export function getSetMintDifficultyDiscriminatorBytes() {
  return getU8Encoder().encode(SET_MINT_DIFFICULTY_DISCRIMINATOR);
}

export type SetMintDifficultyInstruction<
  TProgram extends string = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetMintDifficultyInstructionData = {
  discriminator: number;
  newDifficulty: bigint;
};

export type SetMintDifficultyInstructionDataArgs = {
  newDifficulty: number | bigint;
};

export function getSetMintDifficultyInstructionDataEncoder(): FixedSizeEncoder<SetMintDifficultyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["newDifficulty", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MINT_DIFFICULTY_DISCRIMINATOR }),
  );
}

export function getSetMintDifficultyInstructionDataDecoder(): FixedSizeDecoder<SetMintDifficultyInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["newDifficulty", getU64Decoder()],
  ]);
}

export function getSetMintDifficultyInstructionDataCodec(): FixedSizeCodec<
  SetMintDifficultyInstructionDataArgs,
  SetMintDifficultyInstructionData
> {
  return combineCodec(
    getSetMintDifficultyInstructionDataEncoder(),
    getSetMintDifficultyInstructionDataDecoder(),
  );
}

export type SetMintDifficultyAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  newDifficulty: SetMintDifficultyInstructionDataArgs["newDifficulty"];
};

export async function getSetMintDifficultyInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
>(
  input: SetMintDifficultyAsyncInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMintDifficultyInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDZ_MVP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findGlobalConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.config)],
    data: getSetMintDifficultyInstructionDataEncoder().encode(
      args as SetMintDifficultyInstructionDataArgs,
    ),
    programAddress,
  } as SetMintDifficultyInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type SetMintDifficultyInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  newDifficulty: SetMintDifficultyInstructionDataArgs["newDifficulty"];
};

export function getSetMintDifficultyInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
>(
  input: SetMintDifficultyInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): SetMintDifficultyInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDZ_MVP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.config)],
    data: getSetMintDifficultyInstructionDataEncoder().encode(
      args as SetMintDifficultyInstructionDataArgs,
    ),
    programAddress,
  } as SetMintDifficultyInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type ParsedSetMintDifficultyInstruction<
  TProgram extends string = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetMintDifficultyInstructionData;
};

export function parseSetMintDifficultyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMintDifficultyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), config: getNextAccount() },
    data: getSetMintDifficultyInstructionDataDecoder().decode(instruction.data),
  };
}
