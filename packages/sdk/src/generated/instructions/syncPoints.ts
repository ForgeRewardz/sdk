/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findUserStakePda } from "../pdas";
import { REWARDZ_MVP_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SYNC_POINTS_DISCRIMINATOR = 16;

export function getSyncPointsDiscriminatorBytes() {
  return getU8Encoder().encode(SYNC_POINTS_DISCRIMINATOR);
}

export type SyncPointsInstruction<
  TProgram extends string = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStake extends string | AccountMeta<string> = string,
  TAccountPointRootOrConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? ReadonlySignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStake extends string
        ? WritableAccount<TAccountUserStake>
        : TAccountUserStake,
      TAccountPointRootOrConfig extends string
        ? ReadonlyAccount<TAccountPointRootOrConfig>
        : TAccountPointRootOrConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SyncPointsInstructionData = {
  discriminator: number;
  mode: number;
  amount: bigint;
};

export type SyncPointsInstructionDataArgs = {
  mode: number;
  amount: number | bigint;
};

export function getSyncPointsInstructionDataEncoder(): FixedSizeEncoder<SyncPointsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["mode", getU8Encoder()],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SYNC_POINTS_DISCRIMINATOR }),
  );
}

export function getSyncPointsInstructionDataDecoder(): FixedSizeDecoder<SyncPointsInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["mode", getU8Decoder()],
    ["amount", getU64Decoder()],
  ]);
}

export function getSyncPointsInstructionDataCodec(): FixedSizeCodec<
  SyncPointsInstructionDataArgs,
  SyncPointsInstructionData
> {
  return combineCodec(
    getSyncPointsInstructionDataEncoder(),
    getSyncPointsInstructionDataDecoder(),
  );
}

export type SyncPointsAsyncInput<
  TAccountUser extends string = string,
  TAccountUserStake extends string = string,
  TAccountPointRootOrConfig extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStake?: Address<TAccountUserStake>;
  pointRootOrConfig: Address<TAccountPointRootOrConfig>;
  mode: SyncPointsInstructionDataArgs["mode"];
  amount: SyncPointsInstructionDataArgs["amount"];
};

export async function getSyncPointsInstructionAsync<
  TAccountUser extends string,
  TAccountUserStake extends string,
  TAccountPointRootOrConfig extends string,
  TProgramAddress extends Address = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
>(
  input: SyncPointsAsyncInput<
    TAccountUser,
    TAccountUserStake,
    TAccountPointRootOrConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SyncPointsInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStake,
    TAccountPointRootOrConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDZ_MVP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: false },
    userStake: { value: input.userStake ?? null, isWritable: true },
    pointRootOrConfig: {
      value: input.pointRootOrConfig ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userStake.value) {
    accounts.userStake.value = await findUserStakePda({
      authority: expectAddress(accounts.user.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStake),
      getAccountMeta(accounts.pointRootOrConfig),
    ],
    data: getSyncPointsInstructionDataEncoder().encode(
      args as SyncPointsInstructionDataArgs,
    ),
    programAddress,
  } as SyncPointsInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStake,
    TAccountPointRootOrConfig
  >);
}

export type SyncPointsInput<
  TAccountUser extends string = string,
  TAccountUserStake extends string = string,
  TAccountPointRootOrConfig extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStake: Address<TAccountUserStake>;
  pointRootOrConfig: Address<TAccountPointRootOrConfig>;
  mode: SyncPointsInstructionDataArgs["mode"];
  amount: SyncPointsInstructionDataArgs["amount"];
};

export function getSyncPointsInstruction<
  TAccountUser extends string,
  TAccountUserStake extends string,
  TAccountPointRootOrConfig extends string,
  TProgramAddress extends Address = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
>(
  input: SyncPointsInput<
    TAccountUser,
    TAccountUserStake,
    TAccountPointRootOrConfig
  >,
  config?: { programAddress?: TProgramAddress },
): SyncPointsInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStake,
  TAccountPointRootOrConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? REWARDZ_MVP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: false },
    userStake: { value: input.userStake ?? null, isWritable: true },
    pointRootOrConfig: {
      value: input.pointRootOrConfig ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStake),
      getAccountMeta(accounts.pointRootOrConfig),
    ],
    data: getSyncPointsInstructionDataEncoder().encode(
      args as SyncPointsInstructionDataArgs,
    ),
    programAddress,
  } as SyncPointsInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStake,
    TAccountPointRootOrConfig
  >);
}

export type ParsedSyncPointsInstruction<
  TProgram extends string = typeof REWARDZ_MVP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStake: TAccountMetas[1];
    pointRootOrConfig: TAccountMetas[2];
  };
  data: SyncPointsInstructionData;
};

export function parseSyncPointsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncPointsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStake: getNextAccount(),
      pointRootOrConfig: getNextAccount(),
    },
    data: getSyncPointsInstructionDataDecoder().decode(instruction.data),
  };
}
